# Glass Backend API Environment Variables
# Copy this file to .env and update the values

# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/sunglass_db
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=sunglass_db
POSTGRES_PORT=5432

# Security
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Glass Backend API
VERSION=1.0.0
DESCRIPTION=AI-powered sunglasses detection and marketplace API

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Google Cloud Vision API
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# File Upload
MAX_FILE_SIZE=52428800  # 50MB in bytes
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp

# AI Configuration
AI_CONFIDENCE_THRESHOLD=0.1
USE_AI_MODEL=true
USE_VISION_API=true

# Logging
LOG_LEVEL=INFO
